{
  "scripts": {
    "start-dev": "node start-dev.js",
    "start-backend": "cd backend && pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port 8000",
    "start-frontend": "cd frontend && npm install && npm start"
  }
}
